server {
    listen 80;
    server_name api.jobmatching.ai;

    # Redirect HTTP to HTTPS (uncomment when SSL is ready)
    # return 301 https://$host$request_uri;

    # Gzip settings
    gzip on;
    gzip_types text/plain application/json application/javascript text/css;
    gzip_min_length 1000;
    
    location / {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# SSL configuration (Placeholder for Certbot/Let's Encrypt)
# server {
#     listen 443 ssl;
#     server_name api.jobmatching.ai;
# 
#     ssl_certificate /etc/letsencrypt/live/api.jobmatching.ai/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.jobmatching.ai/privkey.pem;
# 
#     location / {
#         proxy_pass http://api:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
