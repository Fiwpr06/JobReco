{
  "name": "@graphhire/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "lint": "flake8 app/ tests/",
    "test": "pytest"
  }
}
